#include <stdio.h>

int main() {
    FILE *fp;
    
    fp = fopen("sample.txt", "w");

    if (fp == NULL) {
        printf("File not opened\n");
        return 1;
    }

    fprintf(fp, "Hello File Pointer in C");

    fclose(fp);

    printf("Data written successfully\n");
    return 0;
}
